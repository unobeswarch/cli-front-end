// Library root: re-exports the main modules used by the binary.
// `api` contains the HTTP client code that talks to the gateway.
// `ui` contains the interactive, terminal-driven user interface.
pub mod api;
pub mod ui;
